//! fibonacci - we compute the fibonacci sequence recursively

fun fib(n: int) -> int
    if n <= 1 then
        return n
    else
        return fib(n - 1) + fib(n - 2)
    end
end

fib10 := fib(10)
fib1million := fib(1_000_000)
