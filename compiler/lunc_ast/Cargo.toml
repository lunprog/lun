[package]
name = "lunc_ast"
description = "Ast types shared across compiler stages."
version.workspace = true
license.workspace = true
edition = "2024"

[dependencies]
lunc_utils = { workspace = true }

# other dependencies
serde = { workspace = true }
