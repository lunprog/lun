// fun def
main :: fun() {
    // integer literal
    _ = 12;

    // bool literal
    _ = true;
    _ = false;

    // string literal
    _ = "Hello world!";

    // char literal
    _ = 'C';

    // float literal
    _ = 6.022e23;

    // grouping expr
    _ = (12 + 3);

    // ident expr
    _ = main;

    // binary op
    _ = 34 + 35;

    // unary op
    _ = !false;
    _ = -12.0;

    // fun call
    _ = some_fun();
    _ = some_fun(1);
    _ = some_fun(1, 2);
    _ = some_fun(1, 2,);

    // if
    if true {
        main();
    } else {
        main();
    }

    // if then else
    _ = if 23 > 12 then false else true;

    // block
    {
        _ = 2;
        _ = main();
    }

    // while
    while predicate() {
        println("Hello!");
    }

    // for
    for i in range(1, 12) {
        // body
    }

    // return
    return 12 * 8;
    return;

    // continue
    continue;

    // null
    _ = null;

    // deref
    _ = some_pointer.*;

    // types:

    // pointer type
    _ = *u32;
    _ = *mut u32;
}
