// EO08: error testing - MismatchedTypes
a :: fun() -> u8 {}

b : u128 : true;

c :: fun() {
    d : u8 = 12;
    e := -d;
    f := -true;

    g := true.*;
}

i :: fun() -> bool {
    return;
}

j :: fun() {
    k := loop {
        if false {
            break 12;
        }

        break;
    };
}

ops :: fun(v: void) {
    // testing absurd mismatched types in binary ops checking
    a := 1.0 << 1.0;
    b := v * v;
    c := v && v;
    d := 0.0 || 0.2;
    e := 1 || 2;
    f := 1.0 % 2.0;
}
