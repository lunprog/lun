extern "C" {
    puts :: fun(*i8);
}

foo :: fun() -> bool {
    puts(c"foo");
    false
}

bar :: fun() -> bool {
    puts(c"bar");
    true
}

main :: fun() {
    puts(c"BOTH `foo` and `bar`:");
    foo() or bar();
    puts(c"");

    puts(c"ONLY `bar`:");
    bar() or foo();
}
